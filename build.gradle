apply plugin:'java'
jar {
	manifest {
		attributes 'Main-Class': 'main.java.data.MainApp'
	}
	from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  }
}

repositories {
	mavenCentral()
}

dependencies {
	compile 'org.json:json:20160810'
}